[workspace]
resolver = "2"
members = ["xue", "xue-wasm"]
# Experimental native encoder: its own workspace, so this one keeps the
# wasm-tuned release profile below.
exclude = ["xue-encode"]

[profile.release]
lto = true
opt-level = "s"
codegen-units = 1
panic = "abort"
